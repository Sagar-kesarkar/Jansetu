# Infrastructure examples

Backend hosting is undecided; Netlify deployment is deferred. See [deployment preparation](../docs/deployment/DEPLOYMENT.md).

These scripts are legacy examples: deploy_backend.sh targets Cloud Run and requires billing; deploy_frontend.sh targets Firebase Hosting. They are not the approved workflow and have not been validated for this release. Do not execute them under the project's no-billing constraint.
