# Where the console reads casework from. Defaults to a local backend, so
# `npm run dev` works with no .env at all.
#
# For the deployed console point this at the Hugging Face Space, e.g.
#   VITE_API_BASE=https://<user>-jansetu.hf.space
VITE_API_BASE=http://localhost:8080
